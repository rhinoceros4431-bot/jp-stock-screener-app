# 無料で公開する手順(自宅PC + ngrok)

ご自身のPC(Windows/Mac)でアプリを常時起動し、**ngrok** という無料サービスで固定のHTTPS URLを発行して外部公開します。ドメイン購入不要・費用ゼロで、スマホからいつでもアクセスできます。

## この方法の制限(無料プランの範囲)

- 月間 20,000リクエストまで / 月間 帯域幅 1GBまで(個人の利用なら通常十分です)
- **PCの電源が入っていて、アプリとngrokを起動している間だけ**アクセス可能です。PCをシャットダウンすると使えなくなります(スリープでも同様)。常時使いたい場合はPCを常時起動しておく必要があります
- 上記を踏まえ、アプリ側のポーリング間隔は5分に調整済みです(頻繁なリクエストで無料枠を消費しすぎないように)

---

## 手順(スクリプトを使う簡単な方法)

WSL(Ubuntu)またはMac/Linuxのターミナルで、`backend`フォルダに移動してから、以下を**1行ずつコピーして貼り付け→Enter**するだけです。何が起きているかを理解する必要はありません。詰まったら、その時に出たメッセージをそのまま貼ってください。

### 1. Pythonアプリの初期セットアップ(最初の1回だけ)

```bash
cd jp-stock-screener-app/backend
bash setup.sh
```

これだけで「仮想環境の作成→ライブラリのインストール→.envファイルの自動生成」まで全部やってくれます。数分かかります。

### 2. アプリを起動する

```bash
bash start.sh
```

`http://localhost:5000` で動いていることを確認できます(WSLの場合、Windows側のブラウザからも同じアドレスで開けます)。**このターミナルは開いたままにしてください。**

### 3. 別のターミナルを開き、ngrokをインストールする

新しいターミナルウィンドウ(タブ)を開いて、同じ`backend`フォルダに移動してから:

```bash
cd jp-stock-screener-app/backend
bash setup_ngrok.sh
```

インストール後、画面に表示される案内(下記4〜5)に従ってください。

### 4. ngrokの無料アカウントを作る(ブラウザ操作)

1. https://dashboard.ngrok.com/signup を開いて無料登録(メールアドレスのみでOK)
2. https://dashboard.ngrok.com/get-started/your-authtoken を開いてトークンをコピー
3. ターミナルで:
   ```bash
   ngrok config add-authtoken コピーしたトークン
   ```
4. https://dashboard.ngrok.com/domains を開いて「+ Create Domain」をクリックし、発行されたドメイン(例: `xxxx.ngrok-free.app`)をコピー

### 5. トンネルを起動する

```bash
ngrok http --domain=コピーしたドメイン 5000
```

ターミナルに `https://コピーしたドメイン` が表示されます。これがスマホからアクセスするURLです。**このターミナルも開いたままにしてください**(手順2のターミナルと合わせて、2つのターミナルを開いたままにする形になります)。

### 6. スマホでアクセスしてホーム画面に追加

1. スマホのブラウザで手順5で表示された `https://コピーしたドメイン` を開く
2. iPhone: 共有ボタン→「ホーム画面に追加」/ Android: メニュー→「アプリをインストール」
3. アプリを開き、「通知を有効にする」をタップ

---

## まずは手動起動で試してみてください

最初は「2つのターミナルを開いたままにする」運用で十分です(PCを再起動したら、手順2と5をもう一度実行するだけです)。慣れてきて「毎回手動で起動するのが面倒」と感じたら、PC起動時に自動で立ち上がるようにする設定もお手伝いできます(WSLの場合はWindows側のタスクスケジューラから`wsl.exe`経由で起動するやや発展的な設定になります)。その際は改めて教えてください。

---

## トラブルシューティング

- **ngrokのURLにアクセスできない**: `python app.py` と `ngrok http ...` の両方のターミナルが起動したままか確認してください。
- **無料枠(月20,000リクエスト/1GB)を超えた**: ngrokダッシュボードの「Traffic Inspector」で使用量を確認できます。超過した場合は翌月までアクセスできなくなるか、有料プランへの案内が表示されます。ポーリング間隔をさらに伸ばす、または通知は使わずアプリを開いたときだけ確認する運用に切り替えることで消費を抑えられます。
- **PCがスリープしてアクセスできない**: Windowsの電源設定で「スリープしない」に変更してください(電気代は多少かかります)。
